[functions]
  node_bundler = "esbuild"
  directory = "netlify/functions"

[build]
  publish = "public"  # مجلد الملفات النهائي
  command = ""        # لا حاجة لأمر build

[[redirects]]
  from = "/admin/*"
  to = "/admin/index.html"
  status = 200
